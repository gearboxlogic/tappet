apiVersion: tappet.gearboxlogic.dev/v1alpha1
kind: Capability
metadata:
  id: fixture.tie.echo
  name: Shared fixture
  version: 0.1.0
  description: Common equality marker.
  tags: [fixture]
spec:
  parent: fixture.tie
  operations:
    - id: probe
      description: Common equality marker.
      provider: fixture
      target: probe_echo
  providers:
    - id: fixture
      type: mcp
      serverRef: fixture
